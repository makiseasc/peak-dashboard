"use client";

import * as React from "react";

export default function DashboardPage() {
  return (
    <main className="min-h-screen bg-[#0A0A0A] text-white flex flex-col items-center justify-center">
      <h1 className="text-4xl font-bold mb-2">🚀 PEAK DASHBOARD</h1>
      <p className="text-gray-400 text-lg">
        Running clean — Next.js 16 + React 18 initialized successfully.
      </p>
    </main>
  );
}
